apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.appname }}-lb
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 9090
  selector:
    app: {{ .Values.appname }}
